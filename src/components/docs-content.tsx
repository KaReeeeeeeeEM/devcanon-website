import Link from "next/link";
import { CopyCommand } from "@/components/copy-command";

export function InstallationGuide(){return <><p>Choose the installation path that fits how you work. The CLI is available today; desktop builds appear on GitHub Releases as they are published.</p><h2>Before you begin</h2><ul><li>Node.js 20 or newer for CLI installations.</li><li>A terminal with permission to install packages.</li><li>An existing repository, or an empty folder you want to initialize.</li></ul><h2>macOS</h2><h3>Terminal installation</h3><p>Install Node.js with Homebrew if needed, install Devcanon globally, then initialize your repository.</p><div className="docs-commands"><CopyCommand command="brew install node"/><CopyCommand command="npm install --global devcanon"/><CopyCommand command="cd /path/to/project && devcanon init"/></div><h3>Desktop application</h3><p>Open <Link href="https://github.com/KaReeeeeeeeEM/devcanon/releases">GitHub Releases</Link>, download the macOS package matching your processor, open it, and move Devcanon to Applications. If macOS blocks an unsigned preview, verify the release source before using Privacy &amp; Security → Open Anyway.</p><h2>Windows</h2><h3>PowerShell installation</h3><p>Install the current Node.js LTS release, reopen PowerShell, then run:</p><div className="docs-commands"><CopyCommand command="npm install --global devcanon"/><CopyCommand command="cd C:\path\to\project"/><CopyCommand command="devcanon init"/></div><h3>Desktop application</h3><p>Download the Windows installer from <Link href="https://github.com/KaReeeeeeeeEM/devcanon/releases">GitHub Releases</Link>. Verify the publisher and release notes, run the installer, then launch Devcanon from the Start menu.</p><h2>Linux</h2><h3>Terminal installation</h3><p>Use your distribution’s Node.js 20+ package or a version manager, then install and initialize:</p><div className="docs-commands"><CopyCommand command="npm install --global devcanon"/><CopyCommand command="cd /path/to/project && devcanon init"/></div><h3>Desktop application</h3><p>Download the AppImage or distribution package from GitHub Releases. For an AppImage:</p><div className="docs-commands"><CopyCommand command="chmod +x Devcanon-*.AppImage"/><CopyCommand command="./Devcanon-*.AppImage"/></div><h2>Without a global install</h2><p>All operating systems can run Devcanon once through npx:</p><CopyCommand command="npx devcanon init"/><h2>Verify the installation</h2><div className="docs-commands"><CopyCommand command="devcanon --version"/><CopyCommand command="devcanon check"/></div><div className="callout"><strong className="text-foreground">Expected result:</strong> the repository contains <code>.ai/AGENTS.md</code>, the root discovery file, and all required standards. Devcanon reports preserved files rather than silently replacing them.</div></>}

export function CliGuide(){return <><p>Run <code>devcanon</code> without arguments for the interactive prompt, or invoke commands directly in scripts and CI.</p><h2>Initialize a repository</h2><CopyCommand command="devcanon init [path]"/><p>Initialization adds missing standards and preserves local files. Use <code>--no-root-agents</code> only when another discovery mechanism already exists.</p><h2>Interactive shortcuts</h2><ul><li><code>/init [path]</code> — install standards.</li><li><code>/install [path]</code> — alias for init.</li><li><code>/check [path]</code> — validate the handbook.</li><li><code>/update --dry-run</code> — preview packaged changes.</li><li><code>/configure</code> — customize standards interactively.</li><li><code>/cd [path]</code> — change the current target.</li><li><code>/help</code> and <code>/exit</code> — discover or leave.</li></ul><h2>Automation</h2><p>Pin Devcanon as a development dependency and call it from package scripts for reproducible CI.</p><div className="docs-commands"><CopyCommand command="npm install --save-dev devcanon"/><CopyCommand command="npx devcanon check"/></div></>}

export function DesktopGuide(){return <><p>Devcanon Studio provides a visual alternative to terminal configuration while writing the same portable preset format.</p><h2>Download safely</h2><p>Use only the <Link href="https://github.com/KaReeeeeeeeEM/devcanon/releases">official GitHub Releases page</Link>. Select the artifact for macOS, Windows, or Linux and read its version notes before installation.</p><h2>Connect it to a repository</h2><ol><li>Open Devcanon Studio.</li><li>Select an existing repository folder.</li><li>Review detected standards and local differences.</li><li>Configure a preset or individual standards.</li><li>Preview the update, then apply it.</li></ol><h2>CLI compatibility</h2><p>Desktop and CLI workflows use the same <code>.ai</code> structure. You can configure visually, validate with <code>devcanon check</code>, and commit the resulting changes normally.</p><div className="callout">Desktop packages are published progressively. If your operating-system artifact is unavailable, use the CLI or build the Tauri app from the verified source tag.</div></>}

export function PresetsGuide(){return <><p>Presets encode project type, rigor, design direction, density, accent, testing, security, and documentation choices without weakening mandatory safeguards.</p><h2>Create a preset</h2><p>Use the <Link href="/studio">web Studio</Link>, the desktop app, or <code>devcanon configure</code>. Review the expected outcomes before copying the generated code.</p><h2>Install with a preset</h2><CopyCommand command="npx devcanon init --preset dc1_your_code"/><h2>Generated files</h2><ul><li><code>.ai/preset.md</code> explains the choices for humans and agents.</li><li><code>.ai/preset.json</code> stores the machine-readable configuration.</li></ul><p>Commit both files so every contributor and automation environment receives the same decisions.</p></>}

export function UpdatesGuide(){return <><p>Standards updates are reviewable migrations, not blind overwrites.</p><h2>Update Devcanon</h2><div className="docs-commands"><CopyCommand command="npm install --global devcanon@latest"/><CopyCommand command="devcanon update --dry-run"/><CopyCommand command="devcanon update"/><CopyCommand command="devcanon check"/></div><h2>Handle conflicts</h2><p>A conflict means the local standard differs from the packaged version and was preserved. Compare both versions, keep intentional repository rules, and adopt improvements explicitly. Use <code>--force</code> only after review.</p><h2>Team workflow</h2><ol><li>Create a standards-update branch.</li><li>Run the dry run and inspect differences.</li><li>Apply and validate.</li><li>Open a pull request with migration notes.</li><li>Merge only after required checks and review.</li></ol></>}

export function TroubleshootingGuide(){return <><p>Most Devcanon issues come from the current directory, permissions, Node.js versions, or intentionally preserved local files.</p><h2>Filesystem root error</h2><p>Devcanon refuses to install into <code>/</code>. Change to your repository or provide an explicit path.</p><CopyCommand command="cd /path/to/project && devcanon init"/><h2>Command not found</h2><div className="docs-commands"><CopyCommand command="node --version"/><CopyCommand command="npm prefix --global"/><CopyCommand command="npx devcanon --version"/></div><p>Confirm Node.js 20+, reopen the terminal after global installation, or use npx.</p><h2>Conflict is reported</h2><p>This is protective behavior, not data loss. Run <code>devcanon update --dry-run</code>, inspect the local standard, and resolve it in a normal code review.</p><h2>Get help</h2><p>Include your operating system, Node.js version, Devcanon version, command, and sanitized error output in a <Link href="https://github.com/KaReeeeeeeeEM/devcanon/issues">GitHub issue</Link>. Never publish secrets or proprietary source.</p></>}
